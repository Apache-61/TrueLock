# Detection docs

- [`rules.md`](rules.md) — the deterministic fraud-pattern library: what
  we detect, on what inputs, with what method, and why it's high-demo-value.

Implementation lives in `detection/rules/`; the interface every detector
must satisfy is `docs/contracts/detector.md`.
