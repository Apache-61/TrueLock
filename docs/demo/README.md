# Demo docs

- [`runbook.md`](runbook.md) — the three-minute judge flow, including the
  "judge injects a hidden fraud" moment and the Q&A that follows.

See `scripts/demo/` for the executable side of this (loading a scenario,
injecting a hidden fraud pattern, resetting between runs).
