# Investigation docs

- [`protocol.md`](protocol.md) — the bounded agent loop: how a Lead becomes
  a sequence of Investigation Steps, and when it stops.

Implementation lives in `agent/runtime/`; the structured output every step
produces is `docs/contracts/investigation.md`.
