# Dev/test dependencies for the repository-wide baseline (schema/contract
# validation, task-queue checks). Module-specific requirements.txt files
# (backend/, agent/, detection/) are added by the tasks that implement
# those modules.
pytest==8.3.3
