# Runtime dependencies.
#
# Pydantic is the implementation side of the domain contracts: the JSON
# Schema files in `domain/schemas/` are the source of truth, and
# `domain/entities/` mirrors them as validated Python types
# (ARCHITECTURE.md -> "Domain contracts: JSON Schema (+ Pydantic at
# implementation time)").
#
# Pinned, because a validation library that changes its strictness
# between minor versions changes what this system accepts as a valid
# record -- and accepting a malformed record silently is the failure
# `SECURITY.md` is most concerned with.
pydantic==2.13.5
