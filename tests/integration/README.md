Integration tests: detector → lead → tool → evidence, plus PostgreSQL migrations and SQL invariants.

**Requirements:** `psql`, `DATABASE_URL`, and optionally `TRUELOCK_TEST_DATABASE_URL` (defaults to the same value in CI). See `docs/testing.md` and `scripts/check_requirements.sh`.
