{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://truelock/schemas/provider.schema.json",
  "title": "Provider",
  "description": "A supplier/provider entity (RFC-identified), including SAT 69-B/EFOS fiscal status. See docs/contracts/domain.md and research/sat/README.md. EFOS status is fiscal evidence, never an automatic fraud label.",
  "type": "object",
  "required": ["rfc", "name", "efos_status"],
  "additionalProperties": false,
  "properties": {
    "rfc": { "type": "string", "description": "Mexican tax ID (RFC) of the provider." },
    "name": { "type": "string" },
    "registration_date": { "type": "string", "format": "date" },
    "address": { "type": "string" },
    "phone": { "type": "string" },
    "efos_status": {
      "type": "string",
      "enum": ["PRESUMED", "DEFINITIVE", "INVALIDATED", "FAVORABLE_JUDGMENT", "UNKNOWN"],
      "description": "See domain/enums/efos-status.md. Never equated with 'fraud' directly."
    },
    "efos_listed_date": { "type": ["string", "null"], "format": "date" }
  }
}
