# Backlog

Tasks in `RESEARCH`, `PROPOSAL`, or `WAITING_AUTHORIZATION` state — not yet
safe to claim. Empty at bootstrap time; add a file here (from
`tasks/templates/task-template.md`) for anything proposed but not yet
authorized.
