# Active

Tasks in `CLAIMED`, `IN_PROGRESS`, `TESTING`, or `READY_FOR_REVIEW` state.
Empty at bootstrap time. A task file moves here the moment a claim is
verified (`tasks/README.md` → "Claim protocol") — never before.
