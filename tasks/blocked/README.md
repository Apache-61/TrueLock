# Blocked

Tasks that cannot proceed: an unmet dependency, missing information, or an
external outage. Each file should state exactly what's blocking it and
who/what would unblock it — "blocked" without a reason is not useful.
Empty at bootstrap time.
