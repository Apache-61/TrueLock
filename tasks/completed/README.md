# Completed

Tasks in `MERGED` or `VERIFIED` state, plus `REJECTED`/`CANCELLED` tasks
(with the reason noted in the file). Empty at bootstrap time. See
`history/changes/` for the detailed change record that accompanies each
completed task.
